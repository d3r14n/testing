/*
const chai = require('chai');
const expect = chai.expect;

const hora = require('../src/hora');

describe('Hora Actual', () => {
    it('hora', () => {
        expect(hora.time()).to.equal("23:59");
        expect(hora.time()).to.equal("03:33");
        expect(hora.time()).to.equal("14:52");
        expect(hora.time()).to.equal("13:02");
        expect(hora.time()).to.equal("19:24");
        expect(hora.time()).to.equal("10:15");
    });
});
*/